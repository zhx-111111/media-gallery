# Media Gallery — Cloudflare 媒体展示网站

基于 **Cloudflare Workers + D1 + KV** 的零成本媒体展示网站，支持图片、视频、文字的展示与管理。

> 🎨 **Apple 风格 UI** — 毛玻璃、圆角、渐变立体卡片、柔和阴影、流畅动效

## ✨ 功能特性

### 前台展示
- 🖼️ **图片展示** — 渐变立体卡片 + 悬浮浮起效果 + 图片缩放动画
- 🎬 **视频播放** — 支持外链（YouTube / B站 / 任意 mp4 直链）
- 📝 **文字内容** — 优雅排版，支持换行
- 🔍 **搜索筛选** — 按类型、关键词实时搜索
- 🏷️ **自定义分类导航** — 顶部标签可后台自定义名称 + 渐变色
- 📱 **全端适配** — 手机、平板、桌面完美呈现
- 🍎 **Apple 风格** — 毛玻璃导航栏、圆角卡片、SF 字体、渐变标题

### 后台管理
- 🔐 **登录鉴权** — Cookie 会话管理，7 天自动过期
- 👁️ **预览 → 发布工作流** — 保存为草稿 → 预览效果 → 满意后一键发布
- ➕ **添加内容** — 图片上传到 KV / 视频填外链 / 文字直接编辑
- ✏️ **编辑内容** — 修改标题、描述、标签、分类等
- 🗑️ **删除联动释放** — 删除图片时**同步清除 KV 中的二进制数据**，彻底释放存储配额
- 🏷️ **标签系统** — 逗号分隔的多标签管理
- 🏷️ **站点设置** — 后台自定义站点大标题、副标题、分类导航
- 📊 **统计面板** — 草稿数、已发布数、各类型数量 + 图片存储占用
- 📤 **拖拽上传** — 支持拖拽 + 点击上传，带进度条
- 🔒 **私有/公开** — 草稿模式 + 发布控制

## 🆕 本次更新亮点

### 1. 可自定义站点标题
后台「站点设置」Tab → 修改大标题和副标题 → 保存 → 前台立即生效

### 2. 可自定义分类导航
后台「站点设置」→ 添加/删除/重命名分类 → 每个分类可自选颜色 → 自动生成渐变
前台顶部导航标签实时同步

### 3. 渐变立体卡片
- 卡片悬浮时浮起 + 阴影加深
- 图片卡片 hover 图片缓慢放大
- 类型角标改为渐变彩色徽章
- 卡片顶部带分类色条
- 无图卡片显示精美渐变占位

### 4. 预览 → 发布工作流
| 步骤 | 操作 | 前台是否可见 |
|------|------|-------------|
| ① 添加内容 | 填写表单 → 点「👁 预览」看效果 | ❌ 不可见 |
| ② 保存草稿 | 点「💾 保存草稿」 | ❌ 不可见 |
| ③ 预览确认 | 在列表里点「🚀 发布」 | ✅ 立即上线 |

> 删除时仍然**联动清除 KV 图片**，彻底释放存储

## 🏗️ 技术架构

```
┌─────────────────────────────────────────────┐
│          Cloudflare Worker (全栈)          │
│   API + 前台页面 + 后台管理（全部在一个 JS） │
└──────┬──────────────┬──────────────┬───────┘
       │              │              │
       ▼              ▼              ▼
┌──────────┐  ┌──────────────┐  ┌──────────────┐
│  D1      │  │  KV #1 MEDIA │  │  KV #2 CACHE │
│  SQLite  │  │  图片二进制   │  │  会话/缓存   │
│  元数据   │  │  ≤25MB/张   │  │  登录态       │
│  站点设置 │  │  删除即释放   │  │              │
└──────────┘  └──────────────┘  └──────────────┘
```

### 存储分工
| 内容类型 | 存储位置 | 说明 |
|---------|---------|------|
| 图片 | **KV (MEDIA_KV)** | 二进制直存，删除时 `KV.delete()` 彻底释放 |
| 视频 | **外链 URL** | 存于 D1，仅记录地址，不占 CF 存储 |
| 文字 | **D1** | 直接存文本内容 |
| 元数据 | **D1** | 标题、描述、标签、排序等 |
| 站点设置 | **D1** | 标题、副标题、分类配置 |

## 🚀 部署指南

### 前置要求
- Cloudflare 账户（免费版即可）
- Node.js >= 18

### 第一步：创建 Cloudflare 资源

#### 1. 创建 D1 数据库
```bash
cd media-gallery
npx wrangler d1 create media_gallery_db
```
记录输出的 `database_id`。

#### 2. 创建 KV 命名空间（缓存/会话）
```bash
npx wrangler kv namespace create CACHE
```

#### 3. 创建 KV 命名空间（图片存储）
```bash
npx wrangler kv namespace create MEDIA_KV
```

> 你需要**两个 KV 命名空间**：一个存会话缓存，一个存图片二进制。

### 第二步：填写配置

编辑 `wrangler.toml`，填入三个 ID：
```toml
[[d1_databases]]
database_id = "你的 D1 ID"

[[kv_namespaces]]
binding = "CACHE"
id = "你的 KV #1 ID（会话缓存）"

[[kv_namespaces]]
binding = "MEDIA_KV"
id = "你的 KV #2 ID（图片存储）"
```

### 第三步：执行数据库迁移
```bash
npx wrangler d1 migrations apply media_gallery_db
```

### 第四步：部署
```bash
npm install
npm run deploy
```

部署成功后获得 `*.workers.dev` 域名。

### 第五步（可选）：绑定自定义域名
Cloudflare Dashboard → Workers & Pages → 你的 Worker → 设置 → 触发器 → 自定义域

## 📂 项目结构

```
media-gallery/
├── src/
│   └── worker.js          # Worker 主文件（API + 前台 + 后台，Apple UI）
├── migrations/
│   └── 0001_init.sql     # D1 建表 SQL（含 site_settings 表）
├── wrangler.toml          # Cloudflare 配置（D1 + 2×KV，无 R2）
├── package.json
└── README.md
```

## 🔌 API 接口

### 认证
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/login` | 管理员登录 |
| POST | `/api/logout` | 退出 |
| GET  | `/api/auth/check` | 检查登录态 |

### 站点设置
| 方法 | 路径 | 说明 |
|------|------|------|
| GET  | `/api/site-settings` | 获取站点标题、副标题、分类列表 |
| PUT  | `/api/site-settings` | 更新站点设置（后台） |

### 媒体内容
| 方法 | 路径 | 说明 |
|------|------|------|
| GET    | `/api/media?page=1&type=image&search=关键词` | 列表查询（公开） |
| GET    | `/api/media/:id` | 单条详情 |
| POST   | `/api/media` | 创建（默认草稿 is_public=0） |
| PUT    | `/api/media/:id` | 更新 |
| DELETE | `/api/media/:id` | 删除（**联动清除 KV 图片**） |
| POST   | `/api/media/:id/publish` | 发布到前台 |
| POST   | `/api/media/:id/unpublish` | 取消发布（转草稿） |
| POST   | `/api/media/batch-delete` | 批量删除（**联动清除 KV**） |

### 文件上传
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/upload` | 上传图片到 KV（≤24MB） |

### 文件代理
| 方法 | 路径 | 说明 |
|------|------|------|
| GET  | `/file/:key` | 从 KV 读取图片并返回 |

## 🗑️ 删除如何释放存储

后台删除图片时，系统执行**两步清除**：

```js
// 1. 从 KV 彻底删除二进制数据 → 立即释放配额
await env.MEDIA_KV.delete(item.content);
// 2. 从 D1 删除元数据行
await env.DB.prepare('DELETE FROM media_items WHERE id = ?').bind(id).run();
```

> ⚠️ KV 特性提醒：删除后边缘节点最多 60 秒内可能仍命中旧缓存（最终一致性），但**存储配额会立即释放**。

## 💰 成本

Cloudflare 免费额度对个人展示站绰绰有余：

| 资源 | 免费额度 | 本项目用途 |
|------|---------|-----------|
| Workers | 10 万次/天 | API + 页面渲染 |
| D1 | 5GB 存储 | 元数据 + 文字内容 + 站点设置 |
| KV | 1GB 存储 | 图片二进制 + 会话 |
| KV 删除操作 | **免费** | 删除图片不额外收费 |

> 没有 R2，没有出口流量费，零成本运行。

## 🔧 使用说明

### 首次登录
1. 访问 `https://你的域名/login`
2. 用户名 `admin`，密码 `admin123`
3. 进入管理后台

### 自定义站点标题和分类
1. 后台顶部 Tab 切到「⚙ 站点设置」
2. 修改「站点标题」（前台大标题）和「站点副标题」
3. 在分类列表中添加/重命名/删除分类，每个分类可选颜色
4. 点「💾 保存设置」→ 前台立即生效

### 添加内容（预览 → 发布）
1. 后台 → **＋ 添加内容**
2. 选择类型（图片/视频/文字）
3. 填写标题、描述、标签
4. 点「👁 预览」查看效果（右侧实时预览面板）
5. 满意后点「💾 保存草稿」
6. 回到列表，点「🚀 发布」→ 前台立即展示

### 添加图片
1. **＋ 添加内容** → 类型选「图片」
2. 拖拽或点击上传（≤24MB）
3. 预览 → 保存草稿 → 发布

### 添加视频
1. **＋ 添加内容** → 类型选「视频」
2. 在"视频地址"填入外链 URL（YouTube / B站 embed / mp4 直链）
3. 预览 → 保存草稿 → 发布

### 添加文字
1. **＋ 添加内容** → 类型选「文字」
2. 填写标题和文字内容
3. 预览 → 保存草稿 → 发布

### 删除（释放存储）
- 单条删除：点击行末「删除」→ 确认 → 图片从 KV 彻底清除
- 批量删除：勾选多条 → 「🗑 批量删除」→ 确认

### 修改管理员密码
浏览器控制台执行：
```js
crypto.subtle.digest('SHA-256', new TextEncoder().encode('新密码'))
  .then(h => console.log(Array.from(new Uint8Array(h)).map(b => b.toString(16).padStart(2,'0')).join('')));
```
将输出的哈希值更新到 D1：
```sql
UPDATE admins SET password_hash = '哈希值' WHERE username = 'admin';
```

## 🛡️ 安全建议
1. 部署后立即修改默认密码
2. 绑定自定义域名（避免 `*.workers.dev` 暴露信息）
3. 对 `/admin` 和 `/api/*` 路径开启 Cloudflare Access 额外保护
4. KV 命名空间设为私有（默认即私有）

## 📝 License
MIT
