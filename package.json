{
  "name": "media-gallery",
  "version": "7.1.0",
  "description": "Apple-style media gallery on Cloudflare Workers + D1 + KV",
  "main": "src/worker.js",
  "scripts": {
    "build": "echo 'No build step needed — worker.js is plain JS'",
    "check": "node --check src/worker.js"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  },
  "engines": {
    "node": ">=18"
  }
}
