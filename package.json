{
  "name": "media-gallery",
  "version": "5.2.0",
  "description": "Frosted-glass media gallery on Cloudflare Workers + D1 + KV",
  "main": "src/worker.js",
  "scripts": {
    "deploy": "wrangler deploy --env production",
    "migrate": "wrangler d1 migrations apply media_gallery_db --remote",
    "check": "node --check src/worker.js"
  },
  "devDependencies": {
    "wrangler": "4.86.0"
  }
}
